# requirements_molab.txt — all deps for Cost-Aware-Test-Time on Molab (Linux/CUDA)
# Install with:  pip install -r requirements_molab.txt

# Core HTTP + data
requests==2.32.3
datasets==2.20.0
huggingface-hub==0.23.4

# Numerics + analysis
numpy==1.26.4
pandas==2.2.2
scipy==1.13.1

# Plotting (Agg backend, no display needed)
matplotlib==3.9.0
seaborn==0.13.2

# Progress + CLI
tqdm==4.66.4
tabulate==0.9.0
pyyaml==6.0.1

# Hardware monitoring (GPU energy via NVML)
pynvml==11.5.0
psutil==6.0.0

# PDF report generation
reportlab==4.2.2
fpdf2==2.7.9

# SSH / remote deploy (local PC only, harmless to install on Molab too)
paramiko==3.4.0

# PyTorch — CUDA 12.1 wheel (matches Molab's RTX 6000 driver stack)
# Uncomment if you want torch for the strict CUDA check in run_rq2_part1.py
# torch==2.3.1+cu121 --index-url https://download.pytorch.org/whl/cu121
