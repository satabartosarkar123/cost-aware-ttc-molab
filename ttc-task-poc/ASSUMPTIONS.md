# ASSUMPTIONS.md — Design Decisions & Defaults

> Auto-generated by the build agent. Every ambiguity was resolved with a safe default.

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | **Model**: `qwen2.5:3b` (fallback `llama3.2:3b`) | Fits in 6 GB VRAM; good instruction-following for a 3B model. |
| 2 | **Sample size**: 10 questions per task (seed=0) | Small-scale POC; keeps total runtime under ~1 hour on CPU. |
| 3 | **Self-Consistency / BoN k=5** | Scaled down from the paper default (k=40 in SC) due to local computing constraints. |
| 4 | **ToT breadth k=3** | Matches Yao et al. zero-shot BFS configuration. |
| 5 | **Temperature**: 0.0 for greedy, 0.7 for sampling strategies | Standard defaults from the respective papers. |
| 6 | **ORM proxy (Best-of-N)**: LLM-as-a-Judge Outcome Verifier | True ORM training via PRM800K is outside local scope; we prompt the LLM to score outcomes instead (Proxy variant). |
| 7 | **Zero-Shot CoT Protocol** | Explicitly mapping CoT paradigms to Yao et al. Appendix B.1 (Zero-Shot) to prevent 8-shot exemplar context overflow on 3B models. |
| 8 | **Tree of Thought Sampling** | Universal i.i.d sampling applied to all tasks (App B.1). Diverges from Game of 24's official sequential proposal to maintain module unity. |
| 9 | **Game of 24**: diagnostic only, not scored in aggregate accuracy | Small models rarely solve Game of 24; included for qualitative analysis. |
| 10 | **StrategyQA fallback**: ARC-Challenge (`allenai/ai2_arc`, `ARC-Challenge`) | If none of the StrategyQA HF slugs are loadable, ARC-Challenge is used. |
| 11 | **Energy measurement**: GPU Joules via pynvml if available, else CPU % via psutil | Joules require NVIDIA GPU + driver; CPU % is always available. |
| 12 | **Timeout per question**: 120 seconds (strategy-level), 60 seconds per API call | Prevents infinite loops in ToT voting or slow Ollama responses. |
| 13 | **max_tokens**: 1024 for generation, 256 for voting/scoring | Balances output quality vs. speed for a 3B model. |
| 14 | **Virtual environment**: `ttc-task-poc/experiment/.venv` | Standard isolation; falls back to `--user` install if venv creation fails. |
| 15 | **Parse rate threshold**: 0.85 | Below this, a LOW_PARSE_RATE report is generated with failure examples. |
| 16 | **Tie-breaking**: first appearance (SC majority vote), first candidate (BoN equal scores) | Deterministic and reproducible. |
| 17 | **No PRM training**: PRM800K repo is cloned for audit only | Training a Process Reward Model is out of scope for this POC. |
| 18 | **Ollama API endpoint**: `http://localhost:11434` | Default Ollama port; configurable in config.yaml. |
